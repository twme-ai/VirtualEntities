// Generated by MetadataKeysGenerator from entity-data 26.2. Do not edit manually.
package io.github.twme.virtualentities.metadata;

import com.github.retrooper.packetevents.protocol.component.builtin.item.ItemProfile;
import com.github.retrooper.packetevents.protocol.entity.armadillo.ArmadilloState;
import com.github.retrooper.packetevents.protocol.entity.cat.CatSoundVariant;
import com.github.retrooper.packetevents.protocol.entity.cat.CatVariant;
import com.github.retrooper.packetevents.protocol.entity.chicken.ChickenSoundVariant;
import com.github.retrooper.packetevents.protocol.entity.chicken.ChickenVariant;
import com.github.retrooper.packetevents.protocol.entity.cow.CowSoundVariant;
import com.github.retrooper.packetevents.protocol.entity.cow.CowVariant;
import com.github.retrooper.packetevents.protocol.entity.data.EntityDataTypes;
import com.github.retrooper.packetevents.protocol.entity.data.struct.CopperGolemState;
import com.github.retrooper.packetevents.protocol.entity.data.struct.WeatheringCopperState;
import com.github.retrooper.packetevents.protocol.entity.frog.FrogVariant;
import com.github.retrooper.packetevents.protocol.entity.nautilus.ZombieNautilusVariant;
import com.github.retrooper.packetevents.protocol.entity.pig.PigSoundVariant;
import com.github.retrooper.packetevents.protocol.entity.pig.PigVariant;
import com.github.retrooper.packetevents.protocol.entity.pose.EntityPose;
import com.github.retrooper.packetevents.protocol.entity.sniffer.SnifferState;
import com.github.retrooper.packetevents.protocol.entity.villager.VillagerData;
import com.github.retrooper.packetevents.protocol.entity.wolfvariant.WolfSoundVariant;
import com.github.retrooper.packetevents.protocol.entity.wolfvariant.WolfVariant;
import com.github.retrooper.packetevents.protocol.item.ItemStack;
import com.github.retrooper.packetevents.protocol.particle.Particle;
import com.github.retrooper.packetevents.protocol.player.HumanoidArm;
import com.github.retrooper.packetevents.protocol.world.BlockFace;
import com.github.retrooper.packetevents.protocol.world.painting.PaintingVariant;
import com.github.retrooper.packetevents.util.Quaternion4f;
import com.github.retrooper.packetevents.util.Vector3f;
import com.github.retrooper.packetevents.util.Vector3i;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.UUID;
import net.kyori.adventure.text.Component;

/** Typed metadata keys grouped by the declaring Minecraft entity-data class. */
@SuppressWarnings("unused")
public final class GeneratedEntityMetadataKeys {
    private GeneratedEntityMetadataKeys() {
    }

    /** Keys declared by Abstract Arrow in entity-data 26.2. */
    public static class AbstractArrow extends Projectile {
        /** Prevents external instantiation. */
        protected AbstractArrow() {
        }

        /** Metadata field {@code ID_FLAGS}. */
        public static final MetadataKey<Byte> ID_FLAGS = MetadataKey.versioned(
                "ID_FLAGS",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );

        /** Metadata field {@code PIERCE_LEVEL}. */
        public static final MetadataKey<Byte> PIERCE_LEVEL = MetadataKey.versioned(
                "PIERCE_LEVEL",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );

        /** Metadata field {@code IN_GROUND}. */
        public static final MetadataKey<Boolean> IN_GROUND = MetadataKey.versioned(
                "IN_GROUND",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Abstract Boat in entity-data 26.2. */
    public static class AbstractBoat extends VehicleEntity {
        /** Prevents external instantiation. */
        protected AbstractBoat() {
        }

        /** Metadata field {@code PADDLE_LEFT}. */
        public static final MetadataKey<Boolean> PADDLE_LEFT = MetadataKey.versioned(
                "PADDLE_LEFT",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code PADDLE_RIGHT}. */
        public static final MetadataKey<Boolean> PADDLE_RIGHT = MetadataKey.versioned(
                "PADDLE_RIGHT",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code BUBBLE_TIME}. */
        public static final MetadataKey<Integer> BUBBLE_TIME = MetadataKey.versioned(
                "BUBBLE_TIME",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Abstract Chest Boat in entity-data 26.2. */
    public static class AbstractChestBoat extends AbstractBoat {
        /** Prevents external instantiation. */
        protected AbstractChestBoat() {
        }
    }

    /** Keys declared by Abstract Chested Horse in entity-data 26.2. */
    public static class AbstractChestedHorse extends AbstractHorse {
        /** Prevents external instantiation. */
        protected AbstractChestedHorse() {
        }

        /** Metadata field {@code CHEST}. */
        public static final MetadataKey<Boolean> CHEST = MetadataKey.versioned(
                "CHEST",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Abstract Cow in entity-data 26.2. */
    public static class AbstractCow extends Animal {
        /** Prevents external instantiation. */
        protected AbstractCow() {
        }
    }

    /** Keys declared by Abstract Cube Mob in entity-data 26.2. */
    public static class AbstractCubeMob extends AgeableMob {
        /** Prevents external instantiation. */
        protected AbstractCubeMob() {
        }

        /** Metadata field {@code ID_SIZE}. */
        public static final MetadataKey<Integer> ID_SIZE = MetadataKey.versioned(
                "ID_SIZE",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Abstract Fish in entity-data 26.2. */
    public static class AbstractFish extends WaterAnimal {
        /** Prevents external instantiation. */
        protected AbstractFish() {
        }

        /** Metadata field {@code FROM_BUCKET}. */
        public static final MetadataKey<Boolean> FROM_BUCKET = MetadataKey.versioned(
                "FROM_BUCKET",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Abstract Golem in entity-data 26.2. */
    public static class AbstractGolem extends PathfinderMob {
        /** Prevents external instantiation. */
        protected AbstractGolem() {
        }
    }

    /** Keys declared by Abstract Horse in entity-data 26.2. */
    public static class AbstractHorse extends Animal {
        /** Prevents external instantiation. */
        protected AbstractHorse() {
        }

        /** Metadata field {@code FLAGS}. */
        public static final MetadataKey<Byte> FLAGS = MetadataKey.versioned(
                "FLAGS",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );
    }

    /** Keys declared by Abstract Hurting Projectile in entity-data 26.2. */
    public static class AbstractHurtingProjectile extends Projectile {
        /** Prevents external instantiation. */
        protected AbstractHurtingProjectile() {
        }
    }

    /** Keys declared by Abstract Illager in entity-data 26.2. */
    public static class AbstractIllager extends Raider {
        /** Prevents external instantiation. */
        protected AbstractIllager() {
        }
    }

    /** Keys declared by Abstract Minecart in entity-data 26.2. */
    public static class AbstractMinecart extends VehicleEntity {
        /** Prevents external instantiation. */
        protected AbstractMinecart() {
        }

        /** Metadata field {@code CUSTOM_DISPLAY_BLOCK}. */
        public static final MetadataKey<Integer> CUSTOM_DISPLAY_BLOCK = MetadataKey.versioned(
                "CUSTOM_DISPLAY_BLOCK",
                EntityDataTypes.OPTIONAL_BLOCK_STATE,
                Map.ofEntries(
                        Map.entry("Optional<BlockState>", EntityDataTypes.OPTIONAL_BLOCK_STATE)
                )
        );

        /** Metadata field {@code DISPLAY_OFFSET}. */
        public static final MetadataKey<Integer> DISPLAY_OFFSET = MetadataKey.versioned(
                "DISPLAY_OFFSET",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Abstract Minecart Container in entity-data 26.2. */
    public static class AbstractMinecartContainer extends AbstractMinecart {
        /** Prevents external instantiation. */
        protected AbstractMinecartContainer() {
        }
    }

    /** Keys declared by Abstract Nautilus in entity-data 26.2. */
    public static class AbstractNautilus extends TamableAnimal {
        /** Prevents external instantiation. */
        protected AbstractNautilus() {
        }

        /** Metadata field {@code DASH}. */
        public static final MetadataKey<Boolean> DASH = MetadataKey.versioned(
                "DASH",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Abstract Piglin in entity-data 26.2. */
    public static class AbstractPiglin extends Monster {
        /** Prevents external instantiation. */
        protected AbstractPiglin() {
        }

        /** Metadata field {@code IMMUNE_TO_ZOMBIFICATION}. */
        public static final MetadataKey<Boolean> IMMUNE_TO_ZOMBIFICATION = MetadataKey.versioned(
                "IMMUNE_TO_ZOMBIFICATION",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Abstract Schooling Fish in entity-data 26.2. */
    public static class AbstractSchoolingFish extends AbstractFish {
        /** Prevents external instantiation. */
        protected AbstractSchoolingFish() {
        }
    }

    /** Keys declared by Abstract Skeleton in entity-data 26.2. */
    public static class AbstractSkeleton extends Monster {
        /** Prevents external instantiation. */
        protected AbstractSkeleton() {
        }
    }

    /** Keys declared by Abstract Thrown Potion in entity-data 26.2. */
    public static class AbstractThrownPotion extends ThrowableItemProjectile {
        /** Prevents external instantiation. */
        protected AbstractThrownPotion() {
        }
    }

    /** Keys declared by Abstract Villager in entity-data 26.2. */
    public static class AbstractVillager extends AgeableMob {
        /** Prevents external instantiation. */
        protected AbstractVillager() {
        }

        /** Metadata field {@code UNHAPPY_COUNTER}. */
        public static final MetadataKey<Integer> UNHAPPY_COUNTER = MetadataKey.versioned(
                "UNHAPPY_COUNTER",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Abstract Wind Charge in entity-data 26.2. */
    public static class AbstractWindCharge extends AbstractHurtingProjectile {
        /** Prevents external instantiation. */
        protected AbstractWindCharge() {
        }
    }

    /** Keys declared by Ageable Mob in entity-data 26.2. */
    public static class AgeableMob extends PathfinderMob {
        /** Prevents external instantiation. */
        protected AgeableMob() {
        }

        /** Metadata field {@code BABY}. */
        public static final MetadataKey<Boolean> BABY = MetadataKey.versioned(
                "BABY",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code AGE_LOCKED}. */
        public static final MetadataKey<Boolean> AGE_LOCKED = MetadataKey.versioned(
                "AGE_LOCKED",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Ageable Water Creature in entity-data 26.2. */
    public static class AgeableWaterCreature extends AgeableMob {
        /** Prevents external instantiation. */
        protected AgeableWaterCreature() {
        }
    }

    /** Keys declared by Allay in entity-data 26.2. */
    public static class Allay extends PathfinderMob {
        /** Prevents external instantiation. */
        protected Allay() {
        }

        /** Metadata field {@code DANCING}. */
        public static final MetadataKey<Boolean> DANCING = MetadataKey.versioned(
                "DANCING",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code CAN_DUPLICATE}. */
        public static final MetadataKey<Boolean> CAN_DUPLICATE = MetadataKey.versioned(
                "CAN_DUPLICATE",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Ambient Creature in entity-data 26.2. */
    public static class AmbientCreature extends Mob {
        /** Prevents external instantiation. */
        protected AmbientCreature() {
        }
    }

    /** Keys declared by Animal in entity-data 26.2. */
    public static class Animal extends AgeableMob {
        /** Prevents external instantiation. */
        protected Animal() {
        }
    }

    /** Keys declared by Area Effect Cloud in entity-data 26.2. */
    public static class AreaEffectCloud extends Entity {
        /** Prevents external instantiation. */
        protected AreaEffectCloud() {
        }

        /** Metadata field {@code RADIUS}. */
        public static final MetadataKey<Float> RADIUS = MetadataKey.versioned(
                "RADIUS",
                EntityDataTypes.FLOAT,
                Map.ofEntries(
                        Map.entry("Float", EntityDataTypes.FLOAT)
                )
        );

        /** Metadata field {@code WAITING}. */
        public static final MetadataKey<Boolean> WAITING = MetadataKey.versioned(
                "WAITING",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code PARTICLE}. */
        public static final MetadataKey<Particle<?>> PARTICLE = MetadataKey.versioned(
                "PARTICLE",
                EntityDataTypes.PARTICLE,
                Map.ofEntries(
                        Map.entry("ParticleOptions", EntityDataTypes.PARTICLE)
                )
        );
    }

    /** Keys declared by Armadillo in entity-data 26.2. */
    public static class Armadillo extends Animal {
        /** Prevents external instantiation. */
        protected Armadillo() {
        }

        /** Metadata field {@code ARMADILLO_STATE}. */
        public static final MetadataKey<ArmadilloState> ARMADILLO_STATE = MetadataKey.versioned(
                "ARMADILLO_STATE",
                EntityDataTypes.ARMADILLO_STATE,
                Map.ofEntries(
                        Map.entry("Armadillo.ArmadilloState", EntityDataTypes.ARMADILLO_STATE)
                )
        );
    }

    /** Keys declared by Armor Stand in entity-data 26.2. */
    public static class ArmorStand extends LivingEntity {
        /** Prevents external instantiation. */
        protected ArmorStand() {
        }

        /** Metadata field {@code CLIENT_FLAGS}. */
        public static final MetadataKey<Byte> CLIENT_FLAGS = MetadataKey.versioned(
                "CLIENT_FLAGS",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );

        /** Metadata field {@code HEAD_POSE}. */
        public static final MetadataKey<Vector3f> HEAD_POSE = MetadataKey.versioned(
                "HEAD_POSE",
                EntityDataTypes.ROTATION,
                Map.ofEntries(
                        Map.entry("Rotations", EntityDataTypes.ROTATION)
                )
        );

        /** Metadata field {@code BODY_POSE}. */
        public static final MetadataKey<Vector3f> BODY_POSE = MetadataKey.versioned(
                "BODY_POSE",
                EntityDataTypes.ROTATION,
                Map.ofEntries(
                        Map.entry("Rotations", EntityDataTypes.ROTATION)
                )
        );

        /** Metadata field {@code LEFT_ARM_POSE}. */
        public static final MetadataKey<Vector3f> LEFT_ARM_POSE = MetadataKey.versioned(
                "LEFT_ARM_POSE",
                EntityDataTypes.ROTATION,
                Map.ofEntries(
                        Map.entry("Rotations", EntityDataTypes.ROTATION)
                )
        );

        /** Metadata field {@code RIGHT_ARM_POSE}. */
        public static final MetadataKey<Vector3f> RIGHT_ARM_POSE = MetadataKey.versioned(
                "RIGHT_ARM_POSE",
                EntityDataTypes.ROTATION,
                Map.ofEntries(
                        Map.entry("Rotations", EntityDataTypes.ROTATION)
                )
        );

        /** Metadata field {@code LEFT_LEG_POSE}. */
        public static final MetadataKey<Vector3f> LEFT_LEG_POSE = MetadataKey.versioned(
                "LEFT_LEG_POSE",
                EntityDataTypes.ROTATION,
                Map.ofEntries(
                        Map.entry("Rotations", EntityDataTypes.ROTATION)
                )
        );

        /** Metadata field {@code RIGHT_LEG_POSE}. */
        public static final MetadataKey<Vector3f> RIGHT_LEG_POSE = MetadataKey.versioned(
                "RIGHT_LEG_POSE",
                EntityDataTypes.ROTATION,
                Map.ofEntries(
                        Map.entry("Rotations", EntityDataTypes.ROTATION)
                )
        );
    }

    /** Keys declared by Arrow in entity-data 26.2. */
    public static class Arrow extends AbstractArrow {
        /** Prevents external instantiation. */
        protected Arrow() {
        }

        /** Metadata field {@code ID_EFFECT_COLOR}. */
        public static final MetadataKey<Integer> ID_EFFECT_COLOR = MetadataKey.versioned(
                "ID_EFFECT_COLOR",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Avatar in entity-data 26.2. */
    public static class Avatar extends LivingEntity {
        /** Prevents external instantiation. */
        protected Avatar() {
        }

        /** Metadata field {@code PLAYER_MAIN_HAND}. */
        public static final MetadataKey<HumanoidArm> PLAYER_MAIN_HAND = MetadataKey.versioned(
                "PLAYER_MAIN_HAND",
                EntityDataTypes.HUMANOID_ARM,
                Map.ofEntries(
                        Map.entry("HumanoidArm", EntityDataTypes.HUMANOID_ARM)
                )
        );

        /** Metadata field {@code PLAYER_MODE_CUSTOMISATION}. */
        public static final MetadataKey<Byte> PLAYER_MODE_CUSTOMISATION = MetadataKey.versioned(
                "PLAYER_MODE_CUSTOMISATION",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );
    }

    /** Keys declared by Axolotl in entity-data 26.2. */
    public static class Axolotl extends Animal {
        /** Prevents external instantiation. */
        protected Axolotl() {
        }

        /** Metadata field {@code VARIANT}. */
        public static final MetadataKey<Integer> VARIANT = MetadataKey.versioned(
                "VARIANT",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code PLAYING_DEAD}. */
        public static final MetadataKey<Boolean> PLAYING_DEAD = MetadataKey.versioned(
                "PLAYING_DEAD",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code FROM_BUCKET}. */
        public static final MetadataKey<Boolean> FROM_BUCKET = MetadataKey.versioned(
                "FROM_BUCKET",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Bat in entity-data 26.2. */
    public static class Bat extends AmbientCreature {
        /** Prevents external instantiation. */
        protected Bat() {
        }

        /** Metadata field {@code FLAGS}. */
        public static final MetadataKey<Byte> FLAGS = MetadataKey.versioned(
                "FLAGS",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );
    }

    /** Keys declared by Bee in entity-data 26.2. */
    public static class Bee extends Animal {
        /** Prevents external instantiation. */
        protected Bee() {
        }

        /** Metadata field {@code FLAGS}. */
        public static final MetadataKey<Byte> FLAGS = MetadataKey.versioned(
                "FLAGS",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );

        /** Metadata field {@code ANGER_END_TIME}. */
        public static final MetadataKey<Long> ANGER_END_TIME = MetadataKey.versioned(
                "ANGER_END_TIME",
                EntityDataTypes.LONG,
                Map.ofEntries(
                        Map.entry("Long", EntityDataTypes.LONG)
                )
        );
    }

    /** Keys declared by Blaze in entity-data 26.2. */
    public static class Blaze extends Monster {
        /** Prevents external instantiation. */
        protected Blaze() {
        }

        /** Metadata field {@code FLAGS}. */
        public static final MetadataKey<Byte> FLAGS = MetadataKey.versioned(
                "FLAGS",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );
    }

    /** Keys declared by Block Attached Entity in entity-data 26.2. */
    public static class BlockAttachedEntity extends Entity {
        /** Prevents external instantiation. */
        protected BlockAttachedEntity() {
        }
    }

    /** Keys declared by Block Display in entity-data 26.2. */
    public static class BlockDisplay extends Display {
        /** Prevents external instantiation. */
        protected BlockDisplay() {
        }

        /** Metadata field {@code BLOCK_STATE}. */
        public static final MetadataKey<Integer> BLOCK_STATE = MetadataKey.versioned(
                "BLOCK_STATE",
                EntityDataTypes.BLOCK_STATE,
                Map.ofEntries(
                        Map.entry("BlockState", EntityDataTypes.BLOCK_STATE)
                )
        );
    }

    /** Keys declared by Boat in entity-data 26.2. */
    public static class Boat extends AbstractBoat {
        /** Prevents external instantiation. */
        protected Boat() {
        }
    }

    /** Keys declared by Bogged in entity-data 26.2. */
    public static class Bogged extends AbstractSkeleton {
        /** Prevents external instantiation. */
        protected Bogged() {
        }

        /** Metadata field {@code SHEARED}. */
        public static final MetadataKey<Boolean> SHEARED = MetadataKey.versioned(
                "SHEARED",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Breeze in entity-data 26.2. */
    public static class Breeze extends Monster {
        /** Prevents external instantiation. */
        protected Breeze() {
        }
    }

    /** Keys declared by Breeze Wind Charge in entity-data 26.2. */
    public static class BreezeWindCharge extends AbstractWindCharge {
        /** Prevents external instantiation. */
        protected BreezeWindCharge() {
        }
    }

    /** Keys declared by Camel in entity-data 26.2. */
    public static class Camel extends AbstractHorse {
        /** Prevents external instantiation. */
        protected Camel() {
        }

        /** Metadata field {@code DASH}. */
        public static final MetadataKey<Boolean> DASH = MetadataKey.versioned(
                "DASH",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code LAST_POSE_CHANGE_TICK}. */
        public static final MetadataKey<Long> LAST_POSE_CHANGE_TICK = MetadataKey.versioned(
                "LAST_POSE_CHANGE_TICK",
                EntityDataTypes.LONG,
                Map.ofEntries(
                        Map.entry("Long", EntityDataTypes.LONG)
                )
        );
    }

    /** Keys declared by Camel Husk in entity-data 26.2. */
    public static class CamelHusk extends Camel {
        /** Prevents external instantiation. */
        protected CamelHusk() {
        }
    }

    /** Keys declared by Cat in entity-data 26.2. */
    public static class Cat extends TamableAnimal {
        /** Prevents external instantiation. */
        protected Cat() {
        }

        /** Metadata field {@code VARIANT}. */
        public static final MetadataKey<CatVariant> VARIANT = MetadataKey.versioned(
                "VARIANT",
                EntityDataTypes.TYPED_CAT_VARIANT,
                Map.ofEntries(
                        Map.entry("CatVariant", EntityDataTypes.TYPED_CAT_VARIANT),
                        Map.entry("Holder<CatVariant>", EntityDataTypes.TYPED_CAT_VARIANT)
                )
        );

        /** Metadata field {@code IS_LYING}. */
        public static final MetadataKey<Boolean> IS_LYING = MetadataKey.versioned(
                "IS_LYING",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code RELAX_STATE_ONE}. */
        public static final MetadataKey<Boolean> RELAX_STATE_ONE = MetadataKey.versioned(
                "RELAX_STATE_ONE",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code COLLAR_COLOR}. */
        public static final MetadataKey<Integer> COLLAR_COLOR = MetadataKey.versioned(
                "COLLAR_COLOR",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code SOUND_VARIANT}. */
        public static final MetadataKey<CatSoundVariant> SOUND_VARIANT = MetadataKey.versioned(
                "SOUND_VARIANT",
                EntityDataTypes.CAT_SOUND_VARIANT,
                Map.ofEntries(
                        Map.entry("Holder<CatSoundVariant>", EntityDataTypes.CAT_SOUND_VARIANT)
                )
        );
    }

    /** Keys declared by Cave Spider in entity-data 26.2. */
    public static class CaveSpider extends Spider {
        /** Prevents external instantiation. */
        protected CaveSpider() {
        }
    }

    /** Keys declared by Chest Boat in entity-data 26.2. */
    public static class ChestBoat extends AbstractChestBoat {
        /** Prevents external instantiation. */
        protected ChestBoat() {
        }
    }

    /** Keys declared by Chest Raft in entity-data 26.2. */
    public static class ChestRaft extends AbstractChestBoat {
        /** Prevents external instantiation. */
        protected ChestRaft() {
        }
    }

    /** Keys declared by Chicken in entity-data 26.2. */
    public static class Chicken extends Animal {
        /** Prevents external instantiation. */
        protected Chicken() {
        }

        /** Metadata field {@code VARIANT}. */
        public static final MetadataKey<ChickenVariant> VARIANT = MetadataKey.versioned(
                "VARIANT",
                EntityDataTypes.CHICKEN_VARIANT,
                Map.ofEntries(
                        Map.entry("Holder<ChickenVariant>", EntityDataTypes.CHICKEN_VARIANT)
                )
        );

        /** Metadata field {@code SOUND_VARIANT}. */
        public static final MetadataKey<ChickenSoundVariant> SOUND_VARIANT = MetadataKey.versioned(
                "SOUND_VARIANT",
                EntityDataTypes.CHICKEN_SOUND_VARIANT,
                Map.ofEntries(
                        Map.entry("Holder<ChickenSoundVariant>", EntityDataTypes.CHICKEN_SOUND_VARIANT)
                )
        );
    }

    /** Keys declared by Cod in entity-data 26.2. */
    public static class Cod extends AbstractSchoolingFish {
        /** Prevents external instantiation. */
        protected Cod() {
        }
    }

    /** Keys declared by Copper Golem in entity-data 26.2. */
    public static class CopperGolem extends AbstractGolem {
        /** Prevents external instantiation. */
        protected CopperGolem() {
        }

        /** Metadata field {@code WEATHER_STATE}. */
        public static final MetadataKey<WeatheringCopperState> WEATHER_STATE = MetadataKey.versioned(
                "WEATHER_STATE",
                EntityDataTypes.WEATHERING_COPPER_STATE,
                Map.ofEntries(
                        Map.entry("WeatheringCopper.WeatherState", EntityDataTypes.WEATHERING_COPPER_STATE)
                )
        );

        /** Metadata field {@code COPPER_GOLEM_STATE}. */
        public static final MetadataKey<CopperGolemState> COPPER_GOLEM_STATE = MetadataKey.versioned(
                "COPPER_GOLEM_STATE",
                EntityDataTypes.COPPER_GOLEM_STATE,
                Map.ofEntries(
                        Map.entry("CopperGolemState", EntityDataTypes.COPPER_GOLEM_STATE)
                )
        );
    }

    /** Keys declared by Cow in entity-data 26.2. */
    public static class Cow extends AbstractCow {
        /** Prevents external instantiation. */
        protected Cow() {
        }

        /** Metadata field {@code VARIANT}. */
        public static final MetadataKey<CowVariant> VARIANT = MetadataKey.versioned(
                "VARIANT",
                EntityDataTypes.COW_VARIANT,
                Map.ofEntries(
                        Map.entry("Holder<CowVariant>", EntityDataTypes.COW_VARIANT)
                )
        );

        /** Metadata field {@code SOUND_VARIANT}. */
        public static final MetadataKey<CowSoundVariant> SOUND_VARIANT = MetadataKey.versioned(
                "SOUND_VARIANT",
                EntityDataTypes.COW_SOUND_VARIANT,
                Map.ofEntries(
                        Map.entry("Holder<CowSoundVariant>", EntityDataTypes.COW_SOUND_VARIANT)
                )
        );
    }

    /** Keys declared by Creaking in entity-data 26.2. */
    public static class Creaking extends Monster {
        /** Prevents external instantiation. */
        protected Creaking() {
        }

        /** Metadata field {@code CAN_MOVE}. */
        public static final MetadataKey<Boolean> CAN_MOVE = MetadataKey.versioned(
                "CAN_MOVE",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code IS_ACTIVE}. */
        public static final MetadataKey<Boolean> IS_ACTIVE = MetadataKey.versioned(
                "IS_ACTIVE",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code IS_TEARING_DOWN}. */
        public static final MetadataKey<Boolean> IS_TEARING_DOWN = MetadataKey.versioned(
                "IS_TEARING_DOWN",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code HOME_POS}. */
        public static final MetadataKey<Optional<Vector3i>> HOME_POS = MetadataKey.versioned(
                "HOME_POS",
                EntityDataTypes.OPTIONAL_BLOCK_POSITION,
                Map.ofEntries(
                        Map.entry("Optional<BlockPos>", EntityDataTypes.OPTIONAL_BLOCK_POSITION)
                )
        );
    }

    /** Keys declared by Creeper in entity-data 26.2. */
    public static class Creeper extends Monster {
        /** Prevents external instantiation. */
        protected Creeper() {
        }

        /** Metadata field {@code SWELL_DIR}. */
        public static final MetadataKey<Integer> SWELL_DIR = MetadataKey.versioned(
                "SWELL_DIR",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code IS_POWERED}. */
        public static final MetadataKey<Boolean> IS_POWERED = MetadataKey.versioned(
                "IS_POWERED",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code IS_IGNITED}. */
        public static final MetadataKey<Boolean> IS_IGNITED = MetadataKey.versioned(
                "IS_IGNITED",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Display in entity-data 26.2. */
    public static class Display extends Entity {
        /** Prevents external instantiation. */
        protected Display() {
        }

        /** Metadata field {@code TRANSFORMATION_INTERPOLATION_START_DELTA_TICKS}. */
        public static final MetadataKey<Integer> TRANSFORMATION_INTERPOLATION_START_DELTA_TICKS = MetadataKey.versioned(
                "TRANSFORMATION_INTERPOLATION_START_DELTA_TICKS",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code TRANSFORMATION_INTERPOLATION_DURATION}. */
        public static final MetadataKey<Integer> TRANSFORMATION_INTERPOLATION_DURATION = MetadataKey.versioned(
                "TRANSFORMATION_INTERPOLATION_DURATION",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code POS_ROT_INTERPOLATION_DURATION}. */
        public static final MetadataKey<Integer> POS_ROT_INTERPOLATION_DURATION = MetadataKey.versioned(
                "POS_ROT_INTERPOLATION_DURATION",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code TRANSLATION}. */
        public static final MetadataKey<Vector3f> TRANSLATION = MetadataKey.versioned(
                "TRANSLATION",
                EntityDataTypes.VECTOR3F,
                Map.ofEntries(
                        Map.entry("Vector3f", EntityDataTypes.VECTOR3F),
                        Map.entry("Vector3fc", EntityDataTypes.VECTOR3F)
                )
        );

        /** Metadata field {@code SCALE}. */
        public static final MetadataKey<Vector3f> SCALE = MetadataKey.versioned(
                "SCALE",
                EntityDataTypes.VECTOR3F,
                Map.ofEntries(
                        Map.entry("Vector3f", EntityDataTypes.VECTOR3F),
                        Map.entry("Vector3fc", EntityDataTypes.VECTOR3F)
                )
        );

        /** Metadata field {@code LEFT_ROTATION}. */
        public static final MetadataKey<Quaternion4f> LEFT_ROTATION = MetadataKey.versioned(
                "LEFT_ROTATION",
                EntityDataTypes.QUATERNION,
                Map.ofEntries(
                        Map.entry("Quaternionf", EntityDataTypes.QUATERNION),
                        Map.entry("Quaternionfc", EntityDataTypes.QUATERNION)
                )
        );

        /** Metadata field {@code RIGHT_ROTATION}. */
        public static final MetadataKey<Quaternion4f> RIGHT_ROTATION = MetadataKey.versioned(
                "RIGHT_ROTATION",
                EntityDataTypes.QUATERNION,
                Map.ofEntries(
                        Map.entry("Quaternionf", EntityDataTypes.QUATERNION),
                        Map.entry("Quaternionfc", EntityDataTypes.QUATERNION)
                )
        );

        /** Metadata field {@code BILLBOARD_RENDER_CONSTRAINTS}. */
        public static final MetadataKey<Byte> BILLBOARD_RENDER_CONSTRAINTS = MetadataKey.versioned(
                "BILLBOARD_RENDER_CONSTRAINTS",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );

        /** Metadata field {@code BRIGHTNESS_OVERRIDE}. */
        public static final MetadataKey<Integer> BRIGHTNESS_OVERRIDE = MetadataKey.versioned(
                "BRIGHTNESS_OVERRIDE",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code VIEW_RANGE}. */
        public static final MetadataKey<Float> VIEW_RANGE = MetadataKey.versioned(
                "VIEW_RANGE",
                EntityDataTypes.FLOAT,
                Map.ofEntries(
                        Map.entry("Float", EntityDataTypes.FLOAT)
                )
        );

        /** Metadata field {@code SHADOW_RADIUS}. */
        public static final MetadataKey<Float> SHADOW_RADIUS = MetadataKey.versioned(
                "SHADOW_RADIUS",
                EntityDataTypes.FLOAT,
                Map.ofEntries(
                        Map.entry("Float", EntityDataTypes.FLOAT)
                )
        );

        /** Metadata field {@code SHADOW_STRENGTH}. */
        public static final MetadataKey<Float> SHADOW_STRENGTH = MetadataKey.versioned(
                "SHADOW_STRENGTH",
                EntityDataTypes.FLOAT,
                Map.ofEntries(
                        Map.entry("Float", EntityDataTypes.FLOAT)
                )
        );

        /** Metadata field {@code WIDTH}. */
        public static final MetadataKey<Float> WIDTH = MetadataKey.versioned(
                "WIDTH",
                EntityDataTypes.FLOAT,
                Map.ofEntries(
                        Map.entry("Float", EntityDataTypes.FLOAT)
                )
        );

        /** Metadata field {@code HEIGHT}. */
        public static final MetadataKey<Float> HEIGHT = MetadataKey.versioned(
                "HEIGHT",
                EntityDataTypes.FLOAT,
                Map.ofEntries(
                        Map.entry("Float", EntityDataTypes.FLOAT)
                )
        );

        /** Metadata field {@code GLOW_COLOR_OVERRIDE}. */
        public static final MetadataKey<Integer> GLOW_COLOR_OVERRIDE = MetadataKey.versioned(
                "GLOW_COLOR_OVERRIDE",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Dolphin in entity-data 26.2. */
    public static class Dolphin extends AgeableWaterCreature {
        /** Prevents external instantiation. */
        protected Dolphin() {
        }

        /** Metadata field {@code GOT_FISH}. */
        public static final MetadataKey<Boolean> GOT_FISH = MetadataKey.versioned(
                "GOT_FISH",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code MOISTNESS_LEVEL}. */
        public static final MetadataKey<Integer> MOISTNESS_LEVEL = MetadataKey.versioned(
                "MOISTNESS_LEVEL",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Donkey in entity-data 26.2. */
    public static class Donkey extends AbstractChestedHorse {
        /** Prevents external instantiation. */
        protected Donkey() {
        }
    }

    /** Keys declared by Dragon Fireball in entity-data 26.2. */
    public static class DragonFireball extends AbstractHurtingProjectile {
        /** Prevents external instantiation. */
        protected DragonFireball() {
        }
    }

    /** Keys declared by Drowned in entity-data 26.2. */
    public static class Drowned extends Zombie {
        /** Prevents external instantiation. */
        protected Drowned() {
        }
    }

    /** Keys declared by Elder Guardian in entity-data 26.2. */
    public static class ElderGuardian extends Guardian {
        /** Prevents external instantiation. */
        protected ElderGuardian() {
        }
    }

    /** Keys declared by End Crystal in entity-data 26.2. */
    public static class EndCrystal extends Entity {
        /** Prevents external instantiation. */
        protected EndCrystal() {
        }

        /** Metadata field {@code BEAM_TARGET}. */
        public static final MetadataKey<Optional<Vector3i>> BEAM_TARGET = MetadataKey.versioned(
                "BEAM_TARGET",
                EntityDataTypes.OPTIONAL_BLOCK_POSITION,
                Map.ofEntries(
                        Map.entry("Optional<BlockPos>", EntityDataTypes.OPTIONAL_BLOCK_POSITION)
                )
        );

        /** Metadata field {@code SHOW_BOTTOM}. */
        public static final MetadataKey<Boolean> SHOW_BOTTOM = MetadataKey.versioned(
                "SHOW_BOTTOM",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Ender Dragon in entity-data 26.2. */
    public static class EnderDragon extends Mob {
        /** Prevents external instantiation. */
        protected EnderDragon() {
        }

        /** Metadata field {@code PHASE}. */
        public static final MetadataKey<Integer> PHASE = MetadataKey.versioned(
                "PHASE",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Ender Dragon Part in entity-data 26.2. */
    public static class EnderDragonPart extends Entity {
        /** Prevents external instantiation. */
        protected EnderDragonPart() {
        }
    }

    /** Keys declared by Ender Man in entity-data 26.2. */
    public static class EnderMan extends Monster {
        /** Prevents external instantiation. */
        protected EnderMan() {
        }

        /** Metadata field {@code CARRY_STATE}. */
        public static final MetadataKey<Integer> CARRY_STATE = MetadataKey.versioned(
                "CARRY_STATE",
                EntityDataTypes.OPTIONAL_BLOCK_STATE,
                Map.ofEntries(
                        Map.entry("Optional<BlockState>", EntityDataTypes.OPTIONAL_BLOCK_STATE)
                )
        );

        /** Metadata field {@code CREEPY}. */
        public static final MetadataKey<Boolean> CREEPY = MetadataKey.versioned(
                "CREEPY",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code STARED_AT}. */
        public static final MetadataKey<Boolean> STARED_AT = MetadataKey.versioned(
                "STARED_AT",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Endermite in entity-data 26.2. */
    public static class Endermite extends Monster {
        /** Prevents external instantiation. */
        protected Endermite() {
        }
    }

    /** Keys declared by Entity in entity-data 26.2. */
    public static class Entity {
        /** Prevents external instantiation. */
        protected Entity() {
        }

        /** Metadata field {@code SHARED_FLAGS}. */
        public static final MetadataKey<Byte> SHARED_FLAGS = MetadataKey.versioned(
                "SHARED_FLAGS",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );

        /** Metadata field {@code AIR_SUPPLY}. */
        public static final MetadataKey<Integer> AIR_SUPPLY = MetadataKey.versioned(
                "AIR_SUPPLY",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code CUSTOM_NAME}. */
        public static final MetadataKey<Optional<Component>> CUSTOM_NAME = MetadataKey.versioned(
                "CUSTOM_NAME",
                EntityDataTypes.OPTIONAL_ADV_COMPONENT,
                Map.ofEntries(
                        Map.entry("Optional<Component>", EntityDataTypes.OPTIONAL_ADV_COMPONENT),
                        Map.entry("String", EntityDataTypes.STRING)
                )
        );

        /** Metadata field {@code CUSTOM_NAME_VISIBLE}. */
        public static final MetadataKey<Boolean> CUSTOM_NAME_VISIBLE = MetadataKey.versioned(
                "CUSTOM_NAME_VISIBLE",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code SILENT}. */
        public static final MetadataKey<Boolean> SILENT = MetadataKey.versioned(
                "SILENT",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code NO_GRAVITY}. */
        public static final MetadataKey<Boolean> NO_GRAVITY = MetadataKey.versioned(
                "NO_GRAVITY",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code POSE}. */
        public static final MetadataKey<EntityPose> POSE = MetadataKey.versioned(
                "POSE",
                EntityDataTypes.ENTITY_POSE,
                Map.ofEntries(
                        Map.entry("Pose", EntityDataTypes.ENTITY_POSE)
                )
        );

        /** Metadata field {@code TICKS_FROZEN}. */
        public static final MetadataKey<Integer> TICKS_FROZEN = MetadataKey.versioned(
                "TICKS_FROZEN",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Evoker in entity-data 26.2. */
    public static class Evoker extends SpellcasterIllager {
        /** Prevents external instantiation. */
        protected Evoker() {
        }
    }

    /** Keys declared by Evoker Fangs in entity-data 26.2. */
    public static class EvokerFangs extends Entity {
        /** Prevents external instantiation. */
        protected EvokerFangs() {
        }
    }

    /** Keys declared by Experience Orb in entity-data 26.2. */
    public static class ExperienceOrb extends Entity {
        /** Prevents external instantiation. */
        protected ExperienceOrb() {
        }

        /** Metadata field {@code VALUE}. */
        public static final MetadataKey<Integer> VALUE = MetadataKey.versioned(
                "VALUE",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Eye Of Ender in entity-data 26.2. */
    public static class EyeOfEnder extends Entity {
        /** Prevents external instantiation. */
        protected EyeOfEnder() {
        }

        /** Metadata field {@code ITEM_STACK}. */
        public static final MetadataKey<ItemStack> ITEM_STACK = MetadataKey.versioned(
                "ITEM_STACK",
                EntityDataTypes.ITEMSTACK,
                Map.ofEntries(
                        Map.entry("ItemStack", EntityDataTypes.ITEMSTACK)
                )
        );
    }

    /** Keys declared by Falling Block Entity in entity-data 26.2. */
    public static class FallingBlockEntity extends Entity {
        /** Prevents external instantiation. */
        protected FallingBlockEntity() {
        }

        /** Metadata field {@code START_POS}. */
        public static final MetadataKey<Vector3i> START_POS = MetadataKey.versioned(
                "START_POS",
                EntityDataTypes.BLOCK_POSITION,
                Map.ofEntries(
                        Map.entry("BlockPos", EntityDataTypes.BLOCK_POSITION)
                )
        );
    }

    /** Keys declared by Fireball in entity-data 26.2. */
    public static class Fireball extends AbstractHurtingProjectile {
        /** Prevents external instantiation. */
        protected Fireball() {
        }

        /** Metadata field {@code ITEM_STACK}. */
        public static final MetadataKey<ItemStack> ITEM_STACK = MetadataKey.versioned(
                "ITEM_STACK",
                EntityDataTypes.ITEMSTACK,
                Map.ofEntries(
                        Map.entry("ItemStack", EntityDataTypes.ITEMSTACK)
                )
        );
    }

    /** Keys declared by Firework Rocket Entity in entity-data 26.2. */
    public static class FireworkRocketEntity extends Projectile {
        /** Prevents external instantiation. */
        protected FireworkRocketEntity() {
        }

        /** Metadata field {@code FIREWORKS_ITEM}. */
        public static final MetadataKey<ItemStack> FIREWORKS_ITEM = MetadataKey.versioned(
                "FIREWORKS_ITEM",
                EntityDataTypes.ITEMSTACK,
                Map.ofEntries(
                        Map.entry("ItemStack", EntityDataTypes.ITEMSTACK)
                )
        );

        /** Metadata field {@code ATTACHED_TO_TARGET}. */
        public static final MetadataKey<Optional<Integer>> ATTACHED_TO_TARGET = MetadataKey.versioned(
                "ATTACHED_TO_TARGET",
                EntityDataTypes.OPTIONAL_INT,
                Map.ofEntries(
                        Map.entry("OptionalInt", EntityDataTypes.OPTIONAL_INT)
                )
        );

        /** Metadata field {@code SHOT_AT_ANGLE}. */
        public static final MetadataKey<Boolean> SHOT_AT_ANGLE = MetadataKey.versioned(
                "SHOT_AT_ANGLE",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Fishing Hook in entity-data 26.2. */
    public static class FishingHook extends Projectile {
        /** Prevents external instantiation. */
        protected FishingHook() {
        }

        /** Metadata field {@code HOOKED_ENTITY}. */
        public static final MetadataKey<Integer> HOOKED_ENTITY = MetadataKey.versioned(
                "HOOKED_ENTITY",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code BITING}. */
        public static final MetadataKey<Boolean> BITING = MetadataKey.versioned(
                "BITING",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Fox in entity-data 26.2. */
    public static class Fox extends Animal {
        /** Prevents external instantiation. */
        protected Fox() {
        }

        /** Metadata field {@code TYPE}. */
        public static final MetadataKey<Integer> TYPE = MetadataKey.versioned(
                "TYPE",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code FLAGS}. */
        public static final MetadataKey<Byte> FLAGS = MetadataKey.versioned(
                "FLAGS",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );

        /** Metadata field {@code TRUSTED_0}. */
        public static final MetadataKey<Optional<UUID>> TRUSTED_0 = MetadataKey.versioned(
                "TRUSTED_0",
                EntityDataTypes.OPTIONAL_UUID,
                Map.ofEntries(
                        Map.entry("Optional<EntityReference<LivingEntity>>", EntityDataTypes.OPTIONAL_UUID),
                        Map.entry("Optional<UUID>", EntityDataTypes.OPTIONAL_UUID)
                )
        );

        /** Metadata field {@code TRUSTED_1}. */
        public static final MetadataKey<Optional<UUID>> TRUSTED_1 = MetadataKey.versioned(
                "TRUSTED_1",
                EntityDataTypes.OPTIONAL_UUID,
                Map.ofEntries(
                        Map.entry("Optional<EntityReference<LivingEntity>>", EntityDataTypes.OPTIONAL_UUID),
                        Map.entry("Optional<UUID>", EntityDataTypes.OPTIONAL_UUID)
                )
        );
    }

    /** Keys declared by Frog in entity-data 26.2. */
    public static class Frog extends Animal {
        /** Prevents external instantiation. */
        protected Frog() {
        }

        /** Metadata field {@code VARIANT}. */
        public static final MetadataKey<FrogVariant> VARIANT = MetadataKey.versioned(
                "VARIANT",
                EntityDataTypes.TYPED_FROG_VARIANT,
                Map.ofEntries(
                        Map.entry("FrogVariant", EntityDataTypes.TYPED_FROG_VARIANT),
                        Map.entry("Holder<FrogVariant>", EntityDataTypes.TYPED_FROG_VARIANT)
                )
        );

        /** Metadata field {@code TONGUE_TARGET}. */
        public static final MetadataKey<Optional<Integer>> TONGUE_TARGET = MetadataKey.versioned(
                "TONGUE_TARGET",
                EntityDataTypes.OPTIONAL_INT,
                Map.ofEntries(
                        Map.entry("OptionalInt", EntityDataTypes.OPTIONAL_INT)
                )
        );
    }

    /** Keys declared by Ghast in entity-data 26.2. */
    public static class Ghast extends Mob {
        /** Prevents external instantiation. */
        protected Ghast() {
        }

        /** Metadata field {@code IS_CHARGING}. */
        public static final MetadataKey<Boolean> IS_CHARGING = MetadataKey.versioned(
                "IS_CHARGING",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Giant in entity-data 26.2. */
    public static class Giant extends Monster {
        /** Prevents external instantiation. */
        protected Giant() {
        }
    }

    /** Keys declared by Glow Item Frame in entity-data 26.2. */
    public static class GlowItemFrame extends ItemFrame {
        /** Prevents external instantiation. */
        protected GlowItemFrame() {
        }
    }

    /** Keys declared by Glow Squid in entity-data 26.2. */
    public static class GlowSquid extends Squid {
        /** Prevents external instantiation. */
        protected GlowSquid() {
        }

        /** Metadata field {@code DARK_TICKS_REMAINING}. */
        public static final MetadataKey<Integer> DARK_TICKS_REMAINING = MetadataKey.versioned(
                "DARK_TICKS_REMAINING",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Goat in entity-data 26.2. */
    public static class Goat extends Animal {
        /** Prevents external instantiation. */
        protected Goat() {
        }

        /** Metadata field {@code IS_SCREAMING_GOAT}. */
        public static final MetadataKey<Boolean> IS_SCREAMING_GOAT = MetadataKey.versioned(
                "IS_SCREAMING_GOAT",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code HAS_LEFT_HORN}. */
        public static final MetadataKey<Boolean> HAS_LEFT_HORN = MetadataKey.versioned(
                "HAS_LEFT_HORN",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code HAS_RIGHT_HORN}. */
        public static final MetadataKey<Boolean> HAS_RIGHT_HORN = MetadataKey.versioned(
                "HAS_RIGHT_HORN",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Guardian in entity-data 26.2. */
    public static class Guardian extends Monster {
        /** Prevents external instantiation. */
        protected Guardian() {
        }

        /** Metadata field {@code MOVING}. */
        public static final MetadataKey<Boolean> MOVING = MetadataKey.versioned(
                "MOVING",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code ATTACK_TARGET}. */
        public static final MetadataKey<Integer> ATTACK_TARGET = MetadataKey.versioned(
                "ATTACK_TARGET",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Hanging Entity in entity-data 26.2. */
    public static class HangingEntity extends BlockAttachedEntity {
        /** Prevents external instantiation. */
        protected HangingEntity() {
        }

        /** Metadata field {@code DIRECTION}. */
        public static final MetadataKey<BlockFace> DIRECTION = MetadataKey.versioned(
                "DIRECTION",
                EntityDataTypes.BLOCK_FACE,
                Map.ofEntries(
                        Map.entry("Direction", EntityDataTypes.BLOCK_FACE)
                )
        );
    }

    /** Keys declared by Happy Ghast in entity-data 26.2. */
    public static class HappyGhast extends Animal {
        /** Prevents external instantiation. */
        protected HappyGhast() {
        }

        /** Metadata field {@code IS_LEASH_HOLDER}. */
        public static final MetadataKey<Boolean> IS_LEASH_HOLDER = MetadataKey.versioned(
                "IS_LEASH_HOLDER",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code STAYS_STILL}. */
        public static final MetadataKey<Boolean> STAYS_STILL = MetadataKey.versioned(
                "STAYS_STILL",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Hoglin in entity-data 26.2. */
    public static class Hoglin extends Animal {
        /** Prevents external instantiation. */
        protected Hoglin() {
        }

        /** Metadata field {@code IMMUNE_TO_ZOMBIFICATION}. */
        public static final MetadataKey<Boolean> IMMUNE_TO_ZOMBIFICATION = MetadataKey.versioned(
                "IMMUNE_TO_ZOMBIFICATION",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Horse in entity-data 26.2. */
    public static class Horse extends AbstractHorse {
        /** Prevents external instantiation. */
        protected Horse() {
        }

        /** Metadata field {@code TYPE_VARIANT}. */
        public static final MetadataKey<Integer> TYPE_VARIANT = MetadataKey.versioned(
                "TYPE_VARIANT",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Husk in entity-data 26.2. */
    public static class Husk extends Zombie {
        /** Prevents external instantiation. */
        protected Husk() {
        }
    }

    /** Keys declared by Illusioner in entity-data 26.2. */
    public static class Illusioner extends SpellcasterIllager {
        /** Prevents external instantiation. */
        protected Illusioner() {
        }
    }

    /** Keys declared by Interaction in entity-data 26.2. */
    public static class Interaction extends Entity {
        /** Prevents external instantiation. */
        protected Interaction() {
        }

        /** Metadata field {@code WIDTH}. */
        public static final MetadataKey<Float> WIDTH = MetadataKey.versioned(
                "WIDTH",
                EntityDataTypes.FLOAT,
                Map.ofEntries(
                        Map.entry("Float", EntityDataTypes.FLOAT)
                )
        );

        /** Metadata field {@code HEIGHT}. */
        public static final MetadataKey<Float> HEIGHT = MetadataKey.versioned(
                "HEIGHT",
                EntityDataTypes.FLOAT,
                Map.ofEntries(
                        Map.entry("Float", EntityDataTypes.FLOAT)
                )
        );

        /** Metadata field {@code RESPONSE}. */
        public static final MetadataKey<Boolean> RESPONSE = MetadataKey.versioned(
                "RESPONSE",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Iron Golem in entity-data 26.2. */
    public static class IronGolem extends AbstractGolem {
        /** Prevents external instantiation. */
        protected IronGolem() {
        }

        /** Metadata field {@code FLAGS}. */
        public static final MetadataKey<Byte> FLAGS = MetadataKey.versioned(
                "FLAGS",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );
    }

    /** Keys declared by Item Display in entity-data 26.2. */
    public static class ItemDisplay extends Display {
        /** Prevents external instantiation. */
        protected ItemDisplay() {
        }

        /** Metadata field {@code ITEM_STACK}. */
        public static final MetadataKey<ItemStack> ITEM_STACK = MetadataKey.versioned(
                "ITEM_STACK",
                EntityDataTypes.ITEMSTACK,
                Map.ofEntries(
                        Map.entry("ItemStack", EntityDataTypes.ITEMSTACK)
                )
        );

        /** Metadata field {@code ITEM_DISPLAY}. */
        public static final MetadataKey<Byte> ITEM_DISPLAY = MetadataKey.versioned(
                "ITEM_DISPLAY",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );
    }

    /** Keys declared by Item Entity in entity-data 26.2. */
    public static class ItemEntity extends Entity {
        /** Prevents external instantiation. */
        protected ItemEntity() {
        }

        /** Metadata field {@code ITEM}. */
        public static final MetadataKey<ItemStack> ITEM = MetadataKey.versioned(
                "ITEM",
                EntityDataTypes.ITEMSTACK,
                Map.ofEntries(
                        Map.entry("ItemStack", EntityDataTypes.ITEMSTACK)
                )
        );
    }

    /** Keys declared by Item Frame in entity-data 26.2. */
    public static class ItemFrame extends HangingEntity {
        /** Prevents external instantiation. */
        protected ItemFrame() {
        }

        /** Metadata field {@code ITEM}. */
        public static final MetadataKey<ItemStack> ITEM = MetadataKey.versioned(
                "ITEM",
                EntityDataTypes.ITEMSTACK,
                Map.ofEntries(
                        Map.entry("ItemStack", EntityDataTypes.ITEMSTACK)
                )
        );

        /** Metadata field {@code ROTATION}. */
        public static final MetadataKey<Integer> ROTATION = MetadataKey.versioned(
                "ROTATION",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Large Fireball in entity-data 26.2. */
    public static class LargeFireball extends Fireball {
        /** Prevents external instantiation. */
        protected LargeFireball() {
        }
    }

    /** Keys declared by Leash Fence Knot Entity in entity-data 26.2. */
    public static class LeashFenceKnotEntity extends BlockAttachedEntity {
        /** Prevents external instantiation. */
        protected LeashFenceKnotEntity() {
        }
    }

    /** Keys declared by Lightning Bolt in entity-data 26.2. */
    public static class LightningBolt extends Entity {
        /** Prevents external instantiation. */
        protected LightningBolt() {
        }
    }

    /** Keys declared by Living Entity in entity-data 26.2. */
    public static class LivingEntity extends Entity {
        /** Prevents external instantiation. */
        protected LivingEntity() {
        }

        /** Metadata field {@code LIVING_ENTITY_FLAGS}. */
        public static final MetadataKey<Byte> LIVING_ENTITY_FLAGS = MetadataKey.versioned(
                "LIVING_ENTITY_FLAGS",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );

        /** Metadata field {@code HEALTH}. */
        public static final MetadataKey<Float> HEALTH = MetadataKey.versioned(
                "HEALTH",
                EntityDataTypes.FLOAT,
                Map.ofEntries(
                        Map.entry("Float", EntityDataTypes.FLOAT)
                )
        );

        /** Metadata field {@code EFFECT_PARTICLES}. */
        public static final MetadataKey<List<Particle<?>>> EFFECT_PARTICLES = MetadataKey.versioned(
                "EFFECT_PARTICLES",
                EntityDataTypes.PARTICLES,
                Map.ofEntries(
                        Map.entry("List<ParticleOptions>", EntityDataTypes.PARTICLES)
                )
        );

        /** Metadata field {@code EFFECT_AMBIENCE}. */
        public static final MetadataKey<Boolean> EFFECT_AMBIENCE = MetadataKey.versioned(
                "EFFECT_AMBIENCE",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code ARROW_COUNT}. */
        public static final MetadataKey<Integer> ARROW_COUNT = MetadataKey.versioned(
                "ARROW_COUNT",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code STINGER_COUNT}. */
        public static final MetadataKey<Integer> STINGER_COUNT = MetadataKey.versioned(
                "STINGER_COUNT",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code SLEEPING_POS}. */
        public static final MetadataKey<Optional<Vector3i>> SLEEPING_POS = MetadataKey.versioned(
                "SLEEPING_POS",
                EntityDataTypes.OPTIONAL_BLOCK_POSITION,
                Map.ofEntries(
                        Map.entry("Optional<BlockPos>", EntityDataTypes.OPTIONAL_BLOCK_POSITION)
                )
        );
    }

    /** Keys declared by Llama in entity-data 26.2. */
    public static class Llama extends AbstractChestedHorse {
        /** Prevents external instantiation. */
        protected Llama() {
        }

        /** Metadata field {@code STRENGTH}. */
        public static final MetadataKey<Integer> STRENGTH = MetadataKey.versioned(
                "STRENGTH",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code VARIANT}. */
        public static final MetadataKey<Integer> VARIANT = MetadataKey.versioned(
                "VARIANT",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Llama Spit in entity-data 26.2. */
    public static class LlamaSpit extends Projectile {
        /** Prevents external instantiation. */
        protected LlamaSpit() {
        }
    }

    /** Keys declared by Magma Cube in entity-data 26.2. */
    public static class MagmaCube extends AbstractCubeMob {
        /** Prevents external instantiation. */
        protected MagmaCube() {
        }
    }

    /** Keys declared by Mannequin in entity-data 26.2. */
    public static class Mannequin extends Avatar {
        /** Prevents external instantiation. */
        protected Mannequin() {
        }

        /** Metadata field {@code PROFILE}. */
        public static final MetadataKey<ItemProfile> PROFILE = MetadataKey.versioned(
                "PROFILE",
                EntityDataTypes.RESOLVABLE_PROFILE,
                Map.ofEntries(
                        Map.entry("ResolvableProfile", EntityDataTypes.RESOLVABLE_PROFILE)
                )
        );

        /** Metadata field {@code IMMOVABLE}. */
        public static final MetadataKey<Boolean> IMMOVABLE = MetadataKey.versioned(
                "IMMOVABLE",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code DESCRIPTION}. */
        public static final MetadataKey<Optional<Component>> DESCRIPTION = MetadataKey.versioned(
                "DESCRIPTION",
                EntityDataTypes.OPTIONAL_ADV_COMPONENT,
                Map.ofEntries(
                        Map.entry("Optional<Component>", EntityDataTypes.OPTIONAL_ADV_COMPONENT)
                )
        );
    }

    /** Keys declared by Marker in entity-data 26.2. */
    public static class Marker extends Entity {
        /** Prevents external instantiation. */
        protected Marker() {
        }
    }

    /** Keys declared by Minecart in entity-data 26.2. */
    public static class Minecart extends AbstractMinecart {
        /** Prevents external instantiation. */
        protected Minecart() {
        }
    }

    /** Keys declared by Minecart Chest in entity-data 26.2. */
    public static class MinecartChest extends AbstractMinecartContainer {
        /** Prevents external instantiation. */
        protected MinecartChest() {
        }
    }

    /** Keys declared by Minecart Command Block in entity-data 26.2. */
    public static class MinecartCommandBlock extends AbstractMinecart {
        /** Prevents external instantiation. */
        protected MinecartCommandBlock() {
        }

        /** Metadata field {@code COMMAND_NAME}. */
        public static final MetadataKey<String> COMMAND_NAME = MetadataKey.versioned(
                "COMMAND_NAME",
                EntityDataTypes.STRING,
                Map.ofEntries(
                        Map.entry("String", EntityDataTypes.STRING)
                )
        );

        /** Metadata field {@code LAST_OUTPUT}. */
        public static final MetadataKey<Component> LAST_OUTPUT = MetadataKey.versioned(
                "LAST_OUTPUT",
                EntityDataTypes.ADV_COMPONENT,
                Map.ofEntries(
                        Map.entry("Component", EntityDataTypes.ADV_COMPONENT),
                        Map.entry("ITextComponent", EntityDataTypes.ADV_COMPONENT)
                )
        );
    }

    /** Keys declared by Minecart Furnace in entity-data 26.2. */
    public static class MinecartFurnace extends AbstractMinecart {
        /** Prevents external instantiation. */
        protected MinecartFurnace() {
        }

        /** Metadata field {@code FUEL}. */
        public static final MetadataKey<Boolean> FUEL = MetadataKey.versioned(
                "FUEL",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Minecart Hopper in entity-data 26.2. */
    public static class MinecartHopper extends AbstractMinecartContainer {
        /** Prevents external instantiation. */
        protected MinecartHopper() {
        }
    }

    /** Keys declared by Minecart Spawner in entity-data 26.2. */
    public static class MinecartSpawner extends AbstractMinecart {
        /** Prevents external instantiation. */
        protected MinecartSpawner() {
        }
    }

    /** Keys declared by Minecart TNT in entity-data 26.2. */
    public static class MinecartTNT extends AbstractMinecart {
        /** Prevents external instantiation. */
        protected MinecartTNT() {
        }
    }

    /** Keys declared by Mob in entity-data 26.2. */
    public static class Mob extends LivingEntity {
        /** Prevents external instantiation. */
        protected Mob() {
        }

        /** Metadata field {@code MOB_FLAGS}. */
        public static final MetadataKey<Byte> MOB_FLAGS = MetadataKey.versioned(
                "MOB_FLAGS",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );
    }

    /** Keys declared by Monster in entity-data 26.2. */
    public static class Monster extends PathfinderMob {
        /** Prevents external instantiation. */
        protected Monster() {
        }
    }

    /** Keys declared by Mule in entity-data 26.2. */
    public static class Mule extends AbstractChestedHorse {
        /** Prevents external instantiation. */
        protected Mule() {
        }
    }

    /** Keys declared by Mushroom Cow in entity-data 26.2. */
    public static class MushroomCow extends AbstractCow {
        /** Prevents external instantiation. */
        protected MushroomCow() {
        }

        /** Metadata field {@code TYPE}. */
        public static final MetadataKey<Integer> TYPE = MetadataKey.versioned(
                "TYPE",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Nautilus in entity-data 26.2. */
    public static class Nautilus extends AbstractNautilus {
        /** Prevents external instantiation. */
        protected Nautilus() {
        }
    }

    /** Keys declared by Ocelot in entity-data 26.2. */
    public static class Ocelot extends Animal {
        /** Prevents external instantiation. */
        protected Ocelot() {
        }

        /** Metadata field {@code TRUSTING}. */
        public static final MetadataKey<Boolean> TRUSTING = MetadataKey.versioned(
                "TRUSTING",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Ominous Item Spawner in entity-data 26.2. */
    public static class OminousItemSpawner extends Entity {
        /** Prevents external instantiation. */
        protected OminousItemSpawner() {
        }

        /** Metadata field {@code ITEM}. */
        public static final MetadataKey<ItemStack> ITEM = MetadataKey.versioned(
                "ITEM",
                EntityDataTypes.ITEMSTACK,
                Map.ofEntries(
                        Map.entry("ItemStack", EntityDataTypes.ITEMSTACK)
                )
        );
    }

    /** Keys declared by Painting in entity-data 26.2. */
    public static class Painting extends HangingEntity {
        /** Prevents external instantiation. */
        protected Painting() {
        }

        /** Metadata field {@code PAINTING_VARIANT}. */
        public static final MetadataKey<PaintingVariant> PAINTING_VARIANT = MetadataKey.versioned(
                "PAINTING_VARIANT",
                EntityDataTypes.PAINTING_VARIANT,
                Map.ofEntries(
                        Map.entry("Holder<PaintingVariant>", EntityDataTypes.PAINTING_VARIANT)
                )
        );
    }

    /** Keys declared by Panda in entity-data 26.2. */
    public static class Panda extends Animal {
        /** Prevents external instantiation. */
        protected Panda() {
        }

        /** Metadata field {@code UNHAPPY_COUNTER}. */
        public static final MetadataKey<Integer> UNHAPPY_COUNTER = MetadataKey.versioned(
                "UNHAPPY_COUNTER",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code SNEEZE_COUNTER}. */
        public static final MetadataKey<Integer> SNEEZE_COUNTER = MetadataKey.versioned(
                "SNEEZE_COUNTER",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code EAT_COUNTER}. */
        public static final MetadataKey<Integer> EAT_COUNTER = MetadataKey.versioned(
                "EAT_COUNTER",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code MAIN_GENE}. */
        public static final MetadataKey<Byte> MAIN_GENE = MetadataKey.versioned(
                "MAIN_GENE",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );

        /** Metadata field {@code HIDDEN_GENE}. */
        public static final MetadataKey<Byte> HIDDEN_GENE = MetadataKey.versioned(
                "HIDDEN_GENE",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );

        /** Metadata field {@code FLAGS}. */
        public static final MetadataKey<Byte> FLAGS = MetadataKey.versioned(
                "FLAGS",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );
    }

    /** Keys declared by Parched in entity-data 26.2. */
    public static class Parched extends AbstractSkeleton {
        /** Prevents external instantiation. */
        protected Parched() {
        }
    }

    /** Keys declared by Parrot in entity-data 26.2. */
    public static class Parrot extends ShoulderRidingEntity {
        /** Prevents external instantiation. */
        protected Parrot() {
        }

        /** Metadata field {@code VARIANT}. */
        public static final MetadataKey<Integer> VARIANT = MetadataKey.versioned(
                "VARIANT",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Pathfinder Mob in entity-data 26.2. */
    public static class PathfinderMob extends Mob {
        /** Prevents external instantiation. */
        protected PathfinderMob() {
        }
    }

    /** Keys declared by Patrolling Monster in entity-data 26.2. */
    public static class PatrollingMonster extends Monster {
        /** Prevents external instantiation. */
        protected PatrollingMonster() {
        }
    }

    /** Keys declared by Phantom in entity-data 26.2. */
    public static class Phantom extends Mob {
        /** Prevents external instantiation. */
        protected Phantom() {
        }

        /** Metadata field {@code ID_SIZE}. */
        public static final MetadataKey<Integer> ID_SIZE = MetadataKey.versioned(
                "ID_SIZE",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Pig in entity-data 26.2. */
    public static class Pig extends Animal {
        /** Prevents external instantiation. */
        protected Pig() {
        }

        /** Metadata field {@code BOOST_TIME}. */
        public static final MetadataKey<Integer> BOOST_TIME = MetadataKey.versioned(
                "BOOST_TIME",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code VARIANT}. */
        public static final MetadataKey<PigVariant> VARIANT = MetadataKey.versioned(
                "VARIANT",
                EntityDataTypes.PIG_VARIANT,
                Map.ofEntries(
                        Map.entry("Holder<PigVariant>", EntityDataTypes.PIG_VARIANT)
                )
        );

        /** Metadata field {@code SOUND_VARIANT}. */
        public static final MetadataKey<PigSoundVariant> SOUND_VARIANT = MetadataKey.versioned(
                "SOUND_VARIANT",
                EntityDataTypes.PIG_SOUND_VARIANT,
                Map.ofEntries(
                        Map.entry("Holder<PigSoundVariant>", EntityDataTypes.PIG_SOUND_VARIANT)
                )
        );
    }

    /** Keys declared by Piglin in entity-data 26.2. */
    public static class Piglin extends AbstractPiglin {
        /** Prevents external instantiation. */
        protected Piglin() {
        }

        /** Metadata field {@code BABY}. */
        public static final MetadataKey<Boolean> BABY = MetadataKey.versioned(
                "BABY",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code IS_CHARGING_CROSSBOW}. */
        public static final MetadataKey<Boolean> IS_CHARGING_CROSSBOW = MetadataKey.versioned(
                "IS_CHARGING_CROSSBOW",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code IS_DANCING}. */
        public static final MetadataKey<Boolean> IS_DANCING = MetadataKey.versioned(
                "IS_DANCING",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Piglin Brute in entity-data 26.2. */
    public static class PiglinBrute extends AbstractPiglin {
        /** Prevents external instantiation. */
        protected PiglinBrute() {
        }
    }

    /** Keys declared by Pillager in entity-data 26.2. */
    public static class Pillager extends AbstractIllager {
        /** Prevents external instantiation. */
        protected Pillager() {
        }

        /** Metadata field {@code IS_CHARGING_CROSSBOW}. */
        public static final MetadataKey<Boolean> IS_CHARGING_CROSSBOW = MetadataKey.versioned(
                "IS_CHARGING_CROSSBOW",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Player in entity-data 26.2. */
    public static class Player extends Avatar {
        /** Prevents external instantiation. */
        protected Player() {
        }

        /** Metadata field {@code PLAYER_ABSORPTION}. */
        public static final MetadataKey<Float> PLAYER_ABSORPTION = MetadataKey.versioned(
                "PLAYER_ABSORPTION",
                EntityDataTypes.FLOAT,
                Map.ofEntries(
                        Map.entry("Float", EntityDataTypes.FLOAT)
                )
        );

        /** Metadata field {@code SCORE}. */
        public static final MetadataKey<Integer> SCORE = MetadataKey.versioned(
                "SCORE",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code SHOULDER_PARROT_LEFT}. */
        public static final MetadataKey<Optional<Integer>> SHOULDER_PARROT_LEFT = MetadataKey.versioned(
                "SHOULDER_PARROT_LEFT",
                EntityDataTypes.OPTIONAL_INT,
                Map.ofEntries(
                        Map.entry("OptionalInt", EntityDataTypes.OPTIONAL_INT)
                )
        );

        /** Metadata field {@code SHOULDER_PARROT_RIGHT}. */
        public static final MetadataKey<Optional<Integer>> SHOULDER_PARROT_RIGHT = MetadataKey.versioned(
                "SHOULDER_PARROT_RIGHT",
                EntityDataTypes.OPTIONAL_INT,
                Map.ofEntries(
                        Map.entry("OptionalInt", EntityDataTypes.OPTIONAL_INT)
                )
        );
    }

    /** Keys declared by Polar Bear in entity-data 26.2. */
    public static class PolarBear extends Animal {
        /** Prevents external instantiation. */
        protected PolarBear() {
        }

        /** Metadata field {@code STANDING}. */
        public static final MetadataKey<Boolean> STANDING = MetadataKey.versioned(
                "STANDING",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Primed Tnt in entity-data 26.2. */
    public static class PrimedTnt extends Entity {
        /** Prevents external instantiation. */
        protected PrimedTnt() {
        }

        /** Metadata field {@code FUSE}. */
        public static final MetadataKey<Integer> FUSE = MetadataKey.versioned(
                "FUSE",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code BLOCK_STATE}. */
        public static final MetadataKey<Integer> BLOCK_STATE = MetadataKey.versioned(
                "BLOCK_STATE",
                EntityDataTypes.BLOCK_STATE,
                Map.ofEntries(
                        Map.entry("BlockState", EntityDataTypes.BLOCK_STATE)
                )
        );
    }

    /** Keys declared by Projectile in entity-data 26.2. */
    public static class Projectile extends Entity {
        /** Prevents external instantiation. */
        protected Projectile() {
        }
    }

    /** Keys declared by Pufferfish in entity-data 26.2. */
    public static class Pufferfish extends AbstractFish {
        /** Prevents external instantiation. */
        protected Pufferfish() {
        }

        /** Metadata field {@code PUFF_STATE}. */
        public static final MetadataKey<Integer> PUFF_STATE = MetadataKey.versioned(
                "PUFF_STATE",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Rabbit in entity-data 26.2. */
    public static class Rabbit extends Animal {
        /** Prevents external instantiation. */
        protected Rabbit() {
        }

        /** Metadata field {@code TYPE}. */
        public static final MetadataKey<Integer> TYPE = MetadataKey.versioned(
                "TYPE",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Raft in entity-data 26.2. */
    public static class Raft extends AbstractBoat {
        /** Prevents external instantiation. */
        protected Raft() {
        }
    }

    /** Keys declared by Raider in entity-data 26.2. */
    public static class Raider extends PatrollingMonster {
        /** Prevents external instantiation. */
        protected Raider() {
        }

        /** Metadata field {@code IS_CELEBRATING}. */
        public static final MetadataKey<Boolean> IS_CELEBRATING = MetadataKey.versioned(
                "IS_CELEBRATING",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Ravager in entity-data 26.2. */
    public static class Ravager extends Raider {
        /** Prevents external instantiation. */
        protected Ravager() {
        }
    }

    /** Keys declared by Salmon in entity-data 26.2. */
    public static class Salmon extends AbstractSchoolingFish {
        /** Prevents external instantiation. */
        protected Salmon() {
        }

        /** Metadata field {@code TYPE}. */
        public static final MetadataKey<Integer> TYPE = MetadataKey.versioned(
                "TYPE",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Sheep in entity-data 26.2. */
    public static class Sheep extends Animal {
        /** Prevents external instantiation. */
        protected Sheep() {
        }

        /** Metadata field {@code WOOL}. */
        public static final MetadataKey<Byte> WOOL = MetadataKey.versioned(
                "WOOL",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );
    }

    /** Keys declared by Shoulder Riding Entity in entity-data 26.2. */
    public static class ShoulderRidingEntity extends TamableAnimal {
        /** Prevents external instantiation. */
        protected ShoulderRidingEntity() {
        }
    }

    /** Keys declared by Shulker in entity-data 26.2. */
    public static class Shulker extends AbstractGolem {
        /** Prevents external instantiation. */
        protected Shulker() {
        }

        /** Metadata field {@code ATTACH_FACE}. */
        public static final MetadataKey<BlockFace> ATTACH_FACE = MetadataKey.versioned(
                "ATTACH_FACE",
                EntityDataTypes.BLOCK_FACE,
                Map.ofEntries(
                        Map.entry("Direction", EntityDataTypes.BLOCK_FACE),
                        Map.entry("EnumFacing", EntityDataTypes.BLOCK_FACE)
                )
        );

        /** Metadata field {@code PEEK}. */
        public static final MetadataKey<Byte> PEEK = MetadataKey.versioned(
                "PEEK",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );

        /** Metadata field {@code COLOR}. */
        public static final MetadataKey<Byte> COLOR = MetadataKey.versioned(
                "COLOR",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );
    }

    /** Keys declared by Shulker Bullet in entity-data 26.2. */
    public static class ShulkerBullet extends Projectile {
        /** Prevents external instantiation. */
        protected ShulkerBullet() {
        }
    }

    /** Keys declared by Silverfish in entity-data 26.2. */
    public static class Silverfish extends Monster {
        /** Prevents external instantiation. */
        protected Silverfish() {
        }
    }

    /** Keys declared by Skeleton in entity-data 26.2. */
    public static class Skeleton extends AbstractSkeleton {
        /** Prevents external instantiation. */
        protected Skeleton() {
        }

        /** Metadata field {@code STRAY_CONVERSION}. */
        public static final MetadataKey<Boolean> STRAY_CONVERSION = MetadataKey.versioned(
                "STRAY_CONVERSION",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Skeleton Horse in entity-data 26.2. */
    public static class SkeletonHorse extends AbstractHorse {
        /** Prevents external instantiation. */
        protected SkeletonHorse() {
        }
    }

    /** Keys declared by Slime in entity-data 26.2. */
    public static class Slime extends AbstractCubeMob {
        /** Prevents external instantiation. */
        protected Slime() {
        }
    }

    /** Keys declared by Small Fireball in entity-data 26.2. */
    public static class SmallFireball extends Fireball {
        /** Prevents external instantiation. */
        protected SmallFireball() {
        }
    }

    /** Keys declared by Sniffer in entity-data 26.2. */
    public static class Sniffer extends Animal {
        /** Prevents external instantiation. */
        protected Sniffer() {
        }

        /** Metadata field {@code STATE}. */
        public static final MetadataKey<SnifferState> STATE = MetadataKey.versioned(
                "STATE",
                EntityDataTypes.SNIFFER_STATE,
                Map.ofEntries(
                        Map.entry("Sniffer.State", EntityDataTypes.SNIFFER_STATE)
                )
        );

        /** Metadata field {@code DROP_SEED_AT_TICK}. */
        public static final MetadataKey<Integer> DROP_SEED_AT_TICK = MetadataKey.versioned(
                "DROP_SEED_AT_TICK",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Snow Golem in entity-data 26.2. */
    public static class SnowGolem extends AbstractGolem {
        /** Prevents external instantiation. */
        protected SnowGolem() {
        }

        /** Metadata field {@code PUMPKIN}. */
        public static final MetadataKey<Byte> PUMPKIN = MetadataKey.versioned(
                "PUMPKIN",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );
    }

    /** Keys declared by Snowball in entity-data 26.2. */
    public static class Snowball extends ThrowableItemProjectile {
        /** Prevents external instantiation. */
        protected Snowball() {
        }
    }

    /** Keys declared by Spectral Arrow in entity-data 26.2. */
    public static class SpectralArrow extends AbstractArrow {
        /** Prevents external instantiation. */
        protected SpectralArrow() {
        }
    }

    /** Keys declared by Spellcaster Illager in entity-data 26.2. */
    public static class SpellcasterIllager extends AbstractIllager {
        /** Prevents external instantiation. */
        protected SpellcasterIllager() {
        }

        /** Metadata field {@code SPELL_CASTING}. */
        public static final MetadataKey<Byte> SPELL_CASTING = MetadataKey.versioned(
                "SPELL_CASTING",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );
    }

    /** Keys declared by Spider in entity-data 26.2. */
    public static class Spider extends Monster {
        /** Prevents external instantiation. */
        protected Spider() {
        }

        /** Metadata field {@code FLAGS}. */
        public static final MetadataKey<Byte> FLAGS = MetadataKey.versioned(
                "FLAGS",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );
    }

    /** Keys declared by Squid in entity-data 26.2. */
    public static class Squid extends AgeableWaterCreature {
        /** Prevents external instantiation. */
        protected Squid() {
        }
    }

    /** Keys declared by Stray in entity-data 26.2. */
    public static class Stray extends AbstractSkeleton {
        /** Prevents external instantiation. */
        protected Stray() {
        }
    }

    /** Keys declared by Strider in entity-data 26.2. */
    public static class Strider extends Animal {
        /** Prevents external instantiation. */
        protected Strider() {
        }

        /** Metadata field {@code BOOST_TIME}. */
        public static final MetadataKey<Integer> BOOST_TIME = MetadataKey.versioned(
                "BOOST_TIME",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code SUFFOCATING}. */
        public static final MetadataKey<Boolean> SUFFOCATING = MetadataKey.versioned(
                "SUFFOCATING",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Sulfur Cube in entity-data 26.2. */
    public static class SulfurCube extends AbstractCubeMob {
        /** Prevents external instantiation. */
        protected SulfurCube() {
        }

        /** Metadata field {@code MAX_FUSE}. */
        public static final MetadataKey<Integer> MAX_FUSE = MetadataKey.versioned(
                "MAX_FUSE",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code FROM_BUCKET}. */
        public static final MetadataKey<Boolean> FROM_BUCKET = MetadataKey.versioned(
                "FROM_BUCKET",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Tadpole in entity-data 26.2. */
    public static class Tadpole extends AbstractFish {
        /** Prevents external instantiation. */
        protected Tadpole() {
        }

        /** Metadata field {@code AGE_LOCKED}. */
        public static final MetadataKey<Boolean> AGE_LOCKED = MetadataKey.versioned(
                "AGE_LOCKED",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Tamable Animal in entity-data 26.2. */
    public static class TamableAnimal extends Animal {
        /** Prevents external instantiation. */
        protected TamableAnimal() {
        }

        /** Metadata field {@code FLAGS}. */
        public static final MetadataKey<Byte> FLAGS = MetadataKey.versioned(
                "FLAGS",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );

        /** Metadata field {@code OWNERUUID}. */
        public static final MetadataKey<Optional<UUID>> OWNERUUID = MetadataKey.versioned(
                "OWNERUUID",
                EntityDataTypes.OPTIONAL_UUID,
                Map.ofEntries(
                        Map.entry("Optional<EntityReference<LivingEntity>>", EntityDataTypes.OPTIONAL_UUID),
                        Map.entry("Optional<UUID>", EntityDataTypes.OPTIONAL_UUID)
                )
        );
    }

    /** Keys declared by Text Display in entity-data 26.2. */
    public static class TextDisplay extends Display {
        /** Prevents external instantiation. */
        protected TextDisplay() {
        }

        /** Metadata field {@code TEXT}. */
        public static final MetadataKey<Component> TEXT = MetadataKey.versioned(
                "TEXT",
                EntityDataTypes.ADV_COMPONENT,
                Map.ofEntries(
                        Map.entry("Component", EntityDataTypes.ADV_COMPONENT)
                )
        );

        /** Metadata field {@code LINE_WIDTH}. */
        public static final MetadataKey<Integer> LINE_WIDTH = MetadataKey.versioned(
                "LINE_WIDTH",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code BACKGROUND_COLOR}. */
        public static final MetadataKey<Integer> BACKGROUND_COLOR = MetadataKey.versioned(
                "BACKGROUND_COLOR",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code TEXT_OPACITY}. */
        public static final MetadataKey<Byte> TEXT_OPACITY = MetadataKey.versioned(
                "TEXT_OPACITY",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );

        /** Metadata field {@code STYLE_FLAGS}. */
        public static final MetadataKey<Byte> STYLE_FLAGS = MetadataKey.versioned(
                "STYLE_FLAGS",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );
    }

    /** Keys declared by Throwable Item Projectile in entity-data 26.2. */
    public static class ThrowableItemProjectile extends ThrowableProjectile {
        /** Prevents external instantiation. */
        protected ThrowableItemProjectile() {
        }

        /** Metadata field {@code ITEM_STACK}. */
        public static final MetadataKey<ItemStack> ITEM_STACK = MetadataKey.versioned(
                "ITEM_STACK",
                EntityDataTypes.ITEMSTACK,
                Map.ofEntries(
                        Map.entry("ItemStack", EntityDataTypes.ITEMSTACK)
                )
        );
    }

    /** Keys declared by Throwable Projectile in entity-data 26.2. */
    public static class ThrowableProjectile extends Projectile {
        /** Prevents external instantiation. */
        protected ThrowableProjectile() {
        }
    }

    /** Keys declared by Thrown Egg in entity-data 26.2. */
    public static class ThrownEgg extends ThrowableItemProjectile {
        /** Prevents external instantiation. */
        protected ThrownEgg() {
        }
    }

    /** Keys declared by Thrown Enderpearl in entity-data 26.2. */
    public static class ThrownEnderpearl extends ThrowableItemProjectile {
        /** Prevents external instantiation. */
        protected ThrownEnderpearl() {
        }
    }

    /** Keys declared by Thrown Experience Bottle in entity-data 26.2. */
    public static class ThrownExperienceBottle extends ThrowableItemProjectile {
        /** Prevents external instantiation. */
        protected ThrownExperienceBottle() {
        }
    }

    /** Keys declared by Thrown Lingering Potion in entity-data 26.2. */
    public static class ThrownLingeringPotion extends AbstractThrownPotion {
        /** Prevents external instantiation. */
        protected ThrownLingeringPotion() {
        }
    }

    /** Keys declared by Thrown Splash Potion in entity-data 26.2. */
    public static class ThrownSplashPotion extends AbstractThrownPotion {
        /** Prevents external instantiation. */
        protected ThrownSplashPotion() {
        }
    }

    /** Keys declared by Thrown Trident in entity-data 26.2. */
    public static class ThrownTrident extends AbstractArrow {
        /** Prevents external instantiation. */
        protected ThrownTrident() {
        }

        /** Metadata field {@code ID_LOYALTY}. */
        public static final MetadataKey<Byte> ID_LOYALTY = MetadataKey.versioned(
                "ID_LOYALTY",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );

        /** Metadata field {@code ID_FOIL}. */
        public static final MetadataKey<Boolean> ID_FOIL = MetadataKey.versioned(
                "ID_FOIL",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Trader Llama in entity-data 26.2. */
    public static class TraderLlama extends Llama {
        /** Prevents external instantiation. */
        protected TraderLlama() {
        }
    }

    /** Keys declared by Tropical Fish in entity-data 26.2. */
    public static class TropicalFish extends AbstractSchoolingFish {
        /** Prevents external instantiation. */
        protected TropicalFish() {
        }

        /** Metadata field {@code TYPE_VARIANT}. */
        public static final MetadataKey<Integer> TYPE_VARIANT = MetadataKey.versioned(
                "TYPE_VARIANT",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Turtle in entity-data 26.2. */
    public static class Turtle extends Animal {
        /** Prevents external instantiation. */
        protected Turtle() {
        }

        /** Metadata field {@code HAS_EGG}. */
        public static final MetadataKey<Boolean> HAS_EGG = MetadataKey.versioned(
                "HAS_EGG",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code LAYING_EGG}. */
        public static final MetadataKey<Boolean> LAYING_EGG = MetadataKey.versioned(
                "LAYING_EGG",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Vehicle Entity in entity-data 26.2. */
    public static class VehicleEntity extends Entity {
        /** Prevents external instantiation. */
        protected VehicleEntity() {
        }

        /** Metadata field {@code HURT}. */
        public static final MetadataKey<Integer> HURT = MetadataKey.versioned(
                "HURT",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code HURTDIR}. */
        public static final MetadataKey<Integer> HURTDIR = MetadataKey.versioned(
                "HURTDIR",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code DAMAGE}. */
        public static final MetadataKey<Float> DAMAGE = MetadataKey.versioned(
                "DAMAGE",
                EntityDataTypes.FLOAT,
                Map.ofEntries(
                        Map.entry("Float", EntityDataTypes.FLOAT)
                )
        );
    }

    /** Keys declared by Vex in entity-data 26.2. */
    public static class Vex extends Monster {
        /** Prevents external instantiation. */
        protected Vex() {
        }

        /** Metadata field {@code FLAGS}. */
        public static final MetadataKey<Byte> FLAGS = MetadataKey.versioned(
                "FLAGS",
                EntityDataTypes.BYTE,
                Map.ofEntries(
                        Map.entry("Byte", EntityDataTypes.BYTE)
                )
        );
    }

    /** Keys declared by Villager in entity-data 26.2. */
    public static class Villager extends AbstractVillager {
        /** Prevents external instantiation. */
        protected Villager() {
        }

        /** Metadata field {@code VILLAGER_DATA}. */
        public static final MetadataKey<VillagerData> VILLAGER_DATA = MetadataKey.versioned(
                "VILLAGER_DATA",
                EntityDataTypes.VILLAGER_DATA,
                Map.ofEntries(
                        Map.entry("VillagerData", EntityDataTypes.VILLAGER_DATA)
                )
        );

        /** Metadata field {@code VILLAGER_FINALIZED}. */
        public static final MetadataKey<Boolean> VILLAGER_FINALIZED = MetadataKey.versioned(
                "VILLAGER_FINALIZED",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Vindicator in entity-data 26.2. */
    public static class Vindicator extends AbstractIllager {
        /** Prevents external instantiation. */
        protected Vindicator() {
        }
    }

    /** Keys declared by Wandering Trader in entity-data 26.2. */
    public static class WanderingTrader extends AbstractVillager {
        /** Prevents external instantiation. */
        protected WanderingTrader() {
        }
    }

    /** Keys declared by Warden in entity-data 26.2. */
    public static class Warden extends Monster {
        /** Prevents external instantiation. */
        protected Warden() {
        }

        /** Metadata field {@code CLIENT_ANGER_LEVEL}. */
        public static final MetadataKey<Integer> CLIENT_ANGER_LEVEL = MetadataKey.versioned(
                "CLIENT_ANGER_LEVEL",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Water Animal in entity-data 26.2. */
    public static class WaterAnimal extends PathfinderMob {
        /** Prevents external instantiation. */
        protected WaterAnimal() {
        }
    }

    /** Keys declared by Wind Charge in entity-data 26.2. */
    public static class WindCharge extends AbstractWindCharge {
        /** Prevents external instantiation. */
        protected WindCharge() {
        }
    }

    /** Keys declared by Witch in entity-data 26.2. */
    public static class Witch extends Raider {
        /** Prevents external instantiation. */
        protected Witch() {
        }

        /** Metadata field {@code USING_ITEM}. */
        public static final MetadataKey<Boolean> USING_ITEM = MetadataKey.versioned(
                "USING_ITEM",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Wither Boss in entity-data 26.2. */
    public static class WitherBoss extends Monster {
        /** Prevents external instantiation. */
        protected WitherBoss() {
        }

        /** Metadata field {@code TARGET_A}. */
        public static final MetadataKey<Integer> TARGET_A = MetadataKey.versioned(
                "TARGET_A",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code TARGET_B}. */
        public static final MetadataKey<Integer> TARGET_B = MetadataKey.versioned(
                "TARGET_B",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code TARGET_C}. */
        public static final MetadataKey<Integer> TARGET_C = MetadataKey.versioned(
                "TARGET_C",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code INV}. */
        public static final MetadataKey<Integer> INV = MetadataKey.versioned(
                "INV",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );
    }

    /** Keys declared by Wither Skeleton in entity-data 26.2. */
    public static class WitherSkeleton extends AbstractSkeleton {
        /** Prevents external instantiation. */
        protected WitherSkeleton() {
        }
    }

    /** Keys declared by Wither Skull in entity-data 26.2. */
    public static class WitherSkull extends AbstractHurtingProjectile {
        /** Prevents external instantiation. */
        protected WitherSkull() {
        }

        /** Metadata field {@code DANGEROUS}. */
        public static final MetadataKey<Boolean> DANGEROUS = MetadataKey.versioned(
                "DANGEROUS",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Wolf in entity-data 26.2. */
    public static class Wolf extends TamableAnimal {
        /** Prevents external instantiation. */
        protected Wolf() {
        }

        /** Metadata field {@code INTERESTED}. */
        public static final MetadataKey<Boolean> INTERESTED = MetadataKey.versioned(
                "INTERESTED",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code COLLAR_COLOR}. */
        public static final MetadataKey<Integer> COLLAR_COLOR = MetadataKey.versioned(
                "COLLAR_COLOR",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code ANGER_END_TIME}. */
        public static final MetadataKey<Long> ANGER_END_TIME = MetadataKey.versioned(
                "ANGER_END_TIME",
                EntityDataTypes.LONG,
                Map.ofEntries(
                        Map.entry("Long", EntityDataTypes.LONG)
                )
        );

        /** Metadata field {@code VARIANT}. */
        public static final MetadataKey<WolfVariant> VARIANT = MetadataKey.versioned(
                "VARIANT",
                EntityDataTypes.TYPED_WOLF_VARIANT,
                Map.ofEntries(
                        Map.entry("Holder<WolfVariant>", EntityDataTypes.TYPED_WOLF_VARIANT)
                )
        );

        /** Metadata field {@code SOUND_VARIANT}. */
        public static final MetadataKey<WolfSoundVariant> SOUND_VARIANT = MetadataKey.versioned(
                "SOUND_VARIANT",
                EntityDataTypes.WOLF_SOUND_VARIANT,
                Map.ofEntries(
                        Map.entry("Holder<WolfSoundVariant>", EntityDataTypes.WOLF_SOUND_VARIANT)
                )
        );
    }

    /** Keys declared by Zoglin in entity-data 26.2. */
    public static class Zoglin extends Monster {
        /** Prevents external instantiation. */
        protected Zoglin() {
        }

        /** Metadata field {@code BABY}. */
        public static final MetadataKey<Boolean> BABY = MetadataKey.versioned(
                "BABY",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Zombie in entity-data 26.2. */
    public static class Zombie extends Monster {
        /** Prevents external instantiation. */
        protected Zombie() {
        }

        /** Metadata field {@code BABY}. */
        public static final MetadataKey<Boolean> BABY = MetadataKey.versioned(
                "BABY",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code SPECIAL_TYPE}. */
        public static final MetadataKey<Integer> SPECIAL_TYPE = MetadataKey.versioned(
                "SPECIAL_TYPE",
                EntityDataTypes.INT,
                Map.ofEntries(
                        Map.entry("Integer", EntityDataTypes.INT)
                )
        );

        /** Metadata field {@code DROWNED_CONVERSION}. */
        public static final MetadataKey<Boolean> DROWNED_CONVERSION = MetadataKey.versioned(
                "DROWNED_CONVERSION",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Zombie Horse in entity-data 26.2. */
    public static class ZombieHorse extends AbstractHorse {
        /** Prevents external instantiation. */
        protected ZombieHorse() {
        }
    }

    /** Keys declared by Zombie Nautilus in entity-data 26.2. */
    public static class ZombieNautilus extends AbstractNautilus {
        /** Prevents external instantiation. */
        protected ZombieNautilus() {
        }

        /** Metadata field {@code VARIANT}. */
        public static final MetadataKey<ZombieNautilusVariant> VARIANT = MetadataKey.versioned(
                "VARIANT",
                EntityDataTypes.ZOMBIE_NAUTILUS_VARIANT,
                Map.ofEntries(
                        Map.entry("Holder<ZombieNautilusVariant>", EntityDataTypes.ZOMBIE_NAUTILUS_VARIANT)
                )
        );
    }

    /** Keys declared by Zombie Villager in entity-data 26.2. */
    public static class ZombieVillager extends Zombie {
        /** Prevents external instantiation. */
        protected ZombieVillager() {
        }

        /** Metadata field {@code CONVERTING}. */
        public static final MetadataKey<Boolean> CONVERTING = MetadataKey.versioned(
                "CONVERTING",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );

        /** Metadata field {@code VILLAGER_DATA}. */
        public static final MetadataKey<VillagerData> VILLAGER_DATA = MetadataKey.versioned(
                "VILLAGER_DATA",
                EntityDataTypes.VILLAGER_DATA,
                Map.ofEntries(
                        Map.entry("VillagerData", EntityDataTypes.VILLAGER_DATA)
                )
        );

        /** Metadata field {@code VILLAGER_FINALIZED}. */
        public static final MetadataKey<Boolean> VILLAGER_FINALIZED = MetadataKey.versioned(
                "VILLAGER_FINALIZED",
                EntityDataTypes.BOOLEAN,
                Map.ofEntries(
                        Map.entry("Boolean", EntityDataTypes.BOOLEAN)
                )
        );
    }

    /** Keys declared by Zombified Piglin in entity-data 26.2. */
    public static class ZombifiedPiglin extends Zombie {
        /** Prevents external instantiation. */
        protected ZombifiedPiglin() {
        }
    }

}
